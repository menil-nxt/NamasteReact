# Namaste React App

# Parcel

- dev build
- local server
- HMR - (Hot Module Replacement) Queckly changes borwser to what ever changes you are done.
- File watching algorithm - written in C++
- Caching - faster Builds
- Image Optimization
- Minification
- Bundling
- Compress
- Consistent Hashing
- code Splitting
- Differential Bunding - Supprort older Browsers
- Diagnostic - good error messages
- error Handling
- HTTPs
- Tree Shaking - remove unused code
- Different dev and prod bundles

# component Composition - Component inside another component(We called one component inside another component)

# Planning of App

- Name of Website(App)

- Commponents
  - Header
    - logo
    - nav Items
      - About
      - Cart
      - Login & Resister

  - Body
    - Message OR Quotes (any this related Food)
    - Search Bar(Input)
    - restaurant-Container
      - restaurant-cards
        - img
        - Name of Restaurnant
        - cuisines
        - price
        - Off
        - place
        - Rating
  - Footer
    - CopyRight
    - links
    - Address
    - contect

# Two Types of Export/Import

- In React there are tow type of import/export.
  - 1. Default Import/Export - only one export
    - export default component_name
    - import component_name from path_of_component

  - 2. Named Import/Export - Multiple export
    - export const component
    - import { component_name } from path.

# Body Component Search code

    <div className="search">
      <h1 className="slogen">Fast delivery, fresh flavors</h1>
      <div className="search-input">
        <input className="search-bar" type="text" placeholder="Search...." />
        <button className="btn-search">SEARCH</button>
        <button
          className="filter-btn"
          onClick={() => {
            listOfRestaurant = listOfRestaurant.filter(
              (res) => res.info.avgRating > 4.5,
            );
            setListOfRestaurant(listOfRestaurant);
            console.log(listOfRestaurant);
          }}
        >
          Top Rated Restaurant
        </button>
      </div>
    </div>;

# useState() hook

    - whenever state variable update , react triggers riconciliation cycle(re-render component)
    - allways use useState inside component not validate outside component

# useState() hook

    - if you we want to fetch data and showing on UI at that time this useEffect() is called
    - how any time useEffect is called -> if no dependancy array => useEffect call on every render
    - if empty dependancy array -> useEffect call on only intial render
    - if any particular event put into array -> than when that event is trigger at that time only render hepped

# Chunking

    - Chunking means splitting your JavaScript bundle into smaller files called chunks.

# Code Splitting

    - Code Splitting is the technique used to create chunks.

# Dynamic Bundling

    - Bundles/chunks are created and loaded dynamically depending on runtime needs.

# lazy Loading

    - Load something ONLY when it is needed.

    - ex :- const Grocery = lazy(() => import("./components/Grocery")); import like this only for lazy loading. or create chunks

# On Demand Loading

    - Load resources only when requested by user or needed by app.

# put into Suspense Component what ever component you want to make chunks.

      - ex :- <Suspense fallback={<h1>Loading.....</h1>}>
            <Grocery />
          </Suspense>

# ways of writing CSS into are project

    - Sass ans Scss
    - Styled-components
    - Matirial ui
    - Bootstrap
    - Chakra ui
    - Ant Design
    - Tailwind css

# what is postcss -> A tool for transforming CSS with JavaScript

# we are Bulding new NamasteReact Project

# Redux ToolKit

- Install @redux.js/toolkit and react-redux
- Build our store
- connect our store to our app
- Build Slice(cartSlice)
- dispach(action) - write slice -> call reducer function -> update/modify redux store slice
- selector - read updated/modify slice
- immer Library

# Redux - Mutating the state

- vanialla(older) redux => Don't mutate state directly, returning was mandatory
  - const newState = [...state];
  - newState.items.push(action.payload);
  - return newState;

- Redux Toolkit - Usese immer libary behind the scene,
- immer libary is yake your current state and change the state of it or create a copy(draft) of it, and you can mutate the draft state directly,
- and immer will take care of creating a new immutable state for you.
- we HAVE to mutate state directly, returning is NOT mandatory - redux take care of it automatically
  - state.items.push(action.payload);

# Why we can not Diractlly pass state = [] in clearCart Reducer function

- example => originalState = {items:[ "pizza"]}

- clearCart: (state) => { -> state = {items:[ "pizza"]} here state is local variable.
  - console.log(state); // it will return proxyObject basically it will return proxyObject of originalState in redux formate so we can not read it.
    // but for read into redux we have to pass it inside current(state) function this way you can read the state data

  - console.log(current(state));
  - state = []; // it will only change the local variable value as empty array but originalState will remain as it is.
  - state.items.length = 0; // throught this we can replace originalState value with new state value.
    // one another way to do this is
  - return { items: [] }; // this will replace the originalState with new state value.
    }

# Manual testing - feture is working or not go to feture and check what that feture is perfectly work or not but it is not enough.

# Types Of Testing(developer)

- Unit Testing - test react component in isolation - only one component test each time.
- Integration Testing - multiple component make one feture and what that feture is follow right flow when it work's. -> how component is called and how data is passing throught that component.
- End to End Testing (e2e testing) - check from user is lending on website to leave the website what user is do like (user login -> searching restaurant -> add got to cart section of any restaurant -> see items -> add liked items into cart -> on cart page make payment of what ever you what to purchase).
  e2e testing required diffrent type of testing like - Cypress , Puppeteer , Selenium.

# Setting up testing in our app

- Install react testing library
- Install jest
- Install Babel Dependancies
- Configure Babel
- Configure parcel config file to disable default Babel transpilation
- jest configration
- npx create-jest
- npx run --init
- Install jsdom library
- Install @bable/preset-react - to make work in test cases
- Include @bable/preset inside my bable
- Install @testing-library/jest.dom library

# Creating tescases

- create on folder with name of (**tests**) and save all test file with .js or.ts extation -> (**tests**)dunder test method
- OR if you don't want to craete folder and do testing than create anywhere file in your project with name of (header.test.js | header.test.ts | header.spec.js | header.spec.ts) it will be now test file.
